rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product/supplier images: public read (they render in the marketplace),
    // authenticated uploads only.
    match /images/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
